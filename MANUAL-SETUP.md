# Manual Setup Checklist — Vonguul Clarity

Same process as vonguul-picks. Steps that need your accounts/actions:

## 1. GitHub
- [ ] Create a new **private** repo (e.g. `vonguul-clarity`) — separate from
      `Amazon_Affiliate` (that's the home-organization site).
- [ ] Give me the repo URL and I'll push this code.

## 2. Vercel
- [ ] Same account as before is fine — this is just a second, independent
      Project. Dashboard → Add New → Project → select `vonguul-clarity`.
- [ ] Watch for the same lowercase-project-name issue as last time if it
      comes up.
- [ ] Once deployed, send me the `.vercel.app` URL so I can update
      `astro.config.mjs`.

## 3. Amazon Associates
- [ ] Add this site's domain to "Manage Websites" once it's live —
      required by the Associates operating agreement, same as before.
- [ ] Reusing the `Vonguul-20` tag as decided — no new tag needed, though
      you can add a dedicated tracking ID later from Associates Central if
      you want separate analytics per site down the line.

## 4. Pinterest (optional, worth reconsidering for this audience)
- [ ] Human Design / manifestation content does perform on Pinterest
      (journal aesthetics, chart graphics), but this audience may respond
      better to different channels — worth deciding once there's a few
      articles live rather than assuming Pinterest is the right fit by
      default the way it was for home organization.

## 5. Ongoing
- [ ] Same as before — I draft, you review before anything goes live via
      a push to `main`.
